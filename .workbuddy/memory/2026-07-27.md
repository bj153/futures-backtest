# 2026-07-27 工作日志

## stable_reversion 策略 - 修复与全品种回测

### Bug修复
- `stable_reversion.py`：补充 `vwap_dev` 参数初始化（之前调用但未声明，导致回测报错）
- `main.py`：PG_CONFIG 添加 `connect_timeout: 5`，init_db() 包裹 try/except，解决数据库不可用时导入卡死

### 环境搭建
- 创建 venv: `C:/Users/Administrator/.workbuddy/binaries/python/envs/default/`
- 安装依赖: fastapi, uvicorn, psycopg2-binary, lightgbm, scikit-learn, akshare, tushare 等
- 后端成功启动在 localhost:8001

### 参数扫描 (20组, MA609 15m)
- 运行 `tune_stable_reversion.py`，最优参数与策略文件头一致
- 最优: RSI(10/90) ADX<25 SL=2.0xATR TP=3.0xATR → +4,530 / 95笔 / 34.7% / 0.6%DD / SR 0.66
- VWAP过滤提高胜率(38.8%)但交易量减半，总收益下降

### 全品种21合约回测
- 创建 `batch_stable_reversion.py` 和 `batch_stable_reversion_all.py`
- 21品种15m回测完成，12盈9亏，总净收益 -1,582
- 最佳品种: MA609(+3819), jm2609(+2364), eg2609(+2327), fu2609(+1468)
- 最差品种: CF609(-5211), p2609(-3351), bu2609(-2036)
- 策略对品种选择敏感，建议仅用于 MA/jm/eg/fu
- 报告: `batch_results_stable_reversion_all_summary.md`
- 数据: `batch_results_stable_reversion_all.json`

## stable_reversion 前端集成 + 满仓模式参数

### 前端改动
- **SettingsPanel.vue**: 新增 stable_reversion 专属参数（EMA周期/ADX周期/ADX上限/VWAP偏离/止盈ATR倍数/ATR周期）+ 满仓模式开关 + 单笔风险%输入
- **App.vue**: 对应的 ref 变量、props绑定、事件处理、`runBacktest()` 的 `strategy_params` 和顶层 `use_full_position`/`max_risk_pct` 传参

### 后端改动
- `main.py`: 修复 `from ml_api` → `from backend.ml_api` 导入路径，确保从项目根目录启动 uvicorn 能正常工作

### 编译状态
- Vite build 成功，dist/ 已更新
- 改为开发模式：Vite dev server 在 8000，后端 uvicorn 在 8001，前端热更新可用

### 数据库密码更新
- 43.163.192.223 密码改为 ubuntu/ubuntu
- 更新 backend/.env: PG_USER=ubuntu, PG_PASSWORD=ubuntu
- get_contracts_from_db() 加 try/except 容错，DB不可用时返回内置合约列表
- /api/contracts 已恢复，返回10个合约

### 前端默认策略修复
- **App.vue `defaultStrategyCode`**: 从 EM双子星 改为 stable_reversion（用户之前跑的都是 EM 策略）
- **`useFullPosition` 默认值**: `false` → `true`（满仓模式默认开启，不再每次都是 1 手）
- 删除前端 payload 中的 `multiplier: 100`（后端已自动根据合约代码识别乘数）
- 修改后 Vite 热更新自动生效

### 交易记录只有16笔的根因修复
- **VWAP过滤bug**: `vwap_dev=0` 时条件变为 `c < vwap` / `c > vwap`（仍是严格的二值过滤），并非真正禁用。修复为 `vwap_dev > 0` 时才做VWAP偏离判断，否则 `long_deviated=True` / `short_deviated=True`
- **前端参数对齐**: `rsiLongTh` 5→10, `rsiShortTh` 95→90, `slAtrMult` 2.5→2.0, `timeStop` 30→12
- **散点tooltip修复**: shared模式下formatter无法正确识别散点系列，改用per-series tooltip（`headerFormat: ''` + `pointFormat`），悬停三角标时显示 时间/价格/数量/盈亏

### EMA冷启动修复 + 数据加载加速
- **EMA（_ema）初始化修复**: 从 `result = [values[0]] * len(values)` 改为 SMA 预热（前 period 根均线做种子），避免冷启动偏差。对 MA2609 影响不大（前200根在2900附近），换了低开品种效果显著
- **数据加载加速**:
  - 后端 `main.py` 添加内存缓存（`_kline_cache`, TTL 5分钟），同参数重复请求 0ms 返回
  - 前端 `App.vue` 加载数据时不再清空 `chartData`（之前先清空再请求，用户看到空白图表等待3-5秒）

### 策略参数重构：参数绑定策略、移到策略Tab
- **StrategyTab.vue**: 新增 `STRATEGY_PARAM_SPECS` 为 stable_reversion/rsi2_revert/mean_revert/trend_atr_v2/eagle_ladder_k/vwap_revert/streak_revert 7个策略定义各自的参数面板（类型包括 number/switch/select）
- 选择不同策略时自动显示对应的参数组，参数值通过 `emit('update:strategyParams')` 同步到 App.vue
- 支持"重置默认"按钮一键恢复默认值
- **SettingsPanel.vue**: 移除全部策略参数和 EMA快线/慢线，只保留基础设置和仓位管理（满仓+风险上限）
- **App.vue**: 策略参数从多个独立 ref 合并为统一 `strategyParams` 对象，新增 `selectedStrategyFile` 跟踪当前选中的策略文件
- `runBacktest` 直接用 `strategyParams.value` 传给后端，不再逐个映射参数名

### MA2609 各周期月度收益回测（stable_reversion, 满仓3%风险, 4-7月）
| 周期 | 数据量 | 交易笔数 | 净利 | 胜率 | 最大回撤 | 最佳月 | 最差月 |
|------|--------|----------|------|------|----------|--------|--------|
| 1m | 8,000 | 163 | -22,528 | 23.9% | -32.2% | — | 6月-17,788 |
| 5m | 5,129 | 76 | **+35,862** | 28.9% | -20.6% | 5月+13,644 | 6月-2,733 |
| **15m** | 1,710 | 37 | **+52,797** | 29.7% | -21.3% | **7月+35,110** | 4月+370 |
| 30m | 893 | 14 | -21,163 | 21.4% | -27.8% | 6月+16,623 | 7月-27,593 |
| 1h | 522 | 2 | -18,051 | 0% | -18.1% | — | 6月-18,051 |
| 1d | 76 | 0 | 0 | — | 0% | — | — |
- **结论**: 15分钟是最优周期，5分钟次之。30m/1h信号太少不可靠，1m噪声过大。日线数据不足（仅76天）。
- 注意: 1m数据从6/17才开始（仅8000条），其他周期从4/1开始
